# API base URL for DropNShare backend (no trailing slash).
# Use HTTPS for production or the server redirect (http→https) turns POST into GET → "GET method not supported".
# Laravel api.php routes do NOT use CSRF; no token needed.
# Examples:
#   Local:    http://localhost:8000/api
#   Production: https://your-domain.com/api
EXPO_PUBLIC_API_URL=https://your-domain.com/api
